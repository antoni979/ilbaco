[build]
  base = "IlBacoDaSeta"
  command = "npm install && npx expo export --platform web"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Redirección SPA - todas las rutas van a index.html
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
